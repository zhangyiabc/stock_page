# 批量写作流水线（连续写多章）
# 用法: /prose run batch-write.prose
#
# ⚠ 核心设计：逐章调用 write-chapter.prose，每章都是独立的 prose 执行。
# 这样每章的子 agent 都在全新 session 中运行，避免 coordinator 上下文累积溢出。
# 章与章之间的连续性通过 memory-keeper 的持久化文件保证（角色状态、章节摘要、伏笔、钩子等）。

input start_chapter: "起始章节号"
input end_chapter: "结束章节号"
input target_words_per_chapter: "每章目标字数（默认3500）"
input require_approval_chapters: "需要人工确认的章节号列表（逗号分隔，如 1,2,3,30；留空则全自动）"

# 由 coordinator 逐章驱动，每章调用独立的 write-chapter.prose
session: coordinator
  prompt: |
    请执行批量写作任务：从第{start_chapter}章写到第{end_chapter}章。
    每章目标字数：{target_words_per_chapter}
    需要人工确认的章节：{require_approval_chapters}

    **执行规则：**

    1. **逐章串行调用 write-chapter.prose**：
       对每一章，使用 `/prose run write-chapter.prose` 执行完整的单章流水线。
       传入参数：
       - chapter_number = 当前章节号
       - target_words = {target_words_per_chapter}
       - require_approval = 如果当前章节号在 [{require_approval_chapters}] 列表中则为 "true"，否则为 "false"

    2. **严格串行，不可并行**：
       第 N 章的 write-chapter.prose 必须完全执行完毕（包括归档）后，才能开始第 N+1 章。
       因为第 N+1 章的上下文依赖第 N 章归档后更新的角色状态、伏笔和章末钩子。

    3. **每章完成后更新进度**：
       每完成一章后，在 memory/plot/progress.md 中更新：
       - 已完成章节号
       - 六维评分记录
       - 下一章编号
       如果某章被用户跳过，也记录跳过状态。

    4. **异常处理**：
       - 如果某章的 write-chapter.prose 执行失败或中断，记录断点到 progress.md，向用户报告错误并等待指示
       - 不要跳过失败的章节自动继续下一章

    5. **完成后汇报**：
       全部章节完成后，向用户汇报：
       - 完成的章节范围
       - 每章的六维评分概览
       - 被跳过或需要修订的章节（如有）
       - 总字数统计

    现在开始，从第{start_chapter}章开始执行。
