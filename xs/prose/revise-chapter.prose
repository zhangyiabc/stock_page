# 章节修订流水线
# 用法: /prose run revise-chapter.prose

input chapter_number: "需要修订的章节号"
input revision_notes: "修订要求（用户的修改意见）"
input original_text: "原文全文（coordinator 在启动流水线前从自己的 novel/ 目录读取并传入）"

agent memory-keeper:
  model: minimax/MiniMax-M2.5
  prompt: "你是小说数据库管理员。精确查询和返回角色状态、世界设定、伏笔信息、章节原文。"

agent writer:
  model: openrouter/moonshotai/kimi-k2.5
  prompt: "你是小说写手。根据修改意见修订章节正文，保持风格一致。"

agent editor:
  model: zai/glm-5
  prompt: "你是资深编辑。审校修订版正文，确保修改到位且没有引入新问题。在报告开头用 VERDICT: PASS 或 VERDICT: FAIL 标注。"

# Step 1: 获取上下文（原文已由 coordinator 通过 input 传入，不需要 memory-keeper 去找）
context_data = session: memory-keeper
  prompt: |
    为第{chapter_number}章修订准备上下文（原文已通过其他途径提供，不需要你查找）：
    1. 该章涉及角色的当前状态和设定约束
    2. 前后章节摘要（用于检查衔接）
    3. 该章涉及的伏笔（活跃伏笔表中与本章相关的条目）
    4. 当前文风指南要点（从 memory/style/style-guide.md 读取核心规则）
    
    请按以下格式返回：
    === 角色状态 ===
    （角色信息）
    
    === 前后章摘要 ===
    （摘要）
    
    === 相关伏笔 ===
    （伏笔信息）
    
    === 文风指南 ===
    （要点）
    
    ⚠ 上下文控制在 1 万字以内。

# Step 2: 修订（原文和上下文通过 context 传入，无需跨 workspace 读取）
revised = session: writer
  prompt: |
    修订第{chapter_number}章。原文和上下文已通过 context 传入，直接从 context 中读取。
    
    修订要求：
    {revision_notes}
    
    修订时注意：
    - 只改需要改的部分，保持其余部分不变
    - 确保修改不引入新的设定冲突
    - 保持文风一致
    - 修订后执行自检（禁用词/限用词/句式模式）
    - 输出完整的修订后正文（不是只输出改动部分）
  context: { original_text, context_data }

# Step 3: 审校修订版
review = session: editor
  prompt: |
    审校第{chapter_number}章的修订版。修订版正文和上下文已通过 context 传入，直接从 context 中读取，不要尝试从文件系统中查找。
    
    重点检查：
    1. 修订要求是否全部满足
    2. 修改是否引入了新的问题（设定冲突/人设矛盾/时间线错误）
    3. 与前后章节的衔接是否仍然通顺
    4. AI 味检测（修订部分是否有 AI 味）
    5. 修订部分的文风是否与保留部分一致
    
    **输出格式：**
    - 第一行写 `VERDICT: PASS` 或 `VERDICT: FAIL`
    - 如判定 FAIL，列出具体问题和修改建议
    - ⚠ 报告控制在 1500 字以内
  context: { revised, context_data }

# Step 4: 更新记忆（如果修改影响了角色状态或伏笔）
session: memory-keeper
  prompt: |
    第{chapter_number}章已修订。修订版正文和审校报告已通过 context 传入，直接从 context 中读取。
    
    检查修订内容是否影响了：
    1. 角色状态（如有变化则更新角色文件）
    2. 伏笔（如有变化则更新伏笔追踪表）
    3. 章节摘要（如有重大情节变化则更新对应卷分片中的摘要）
    4. 时间线（如有事件变化则更新卷时间线分片）
    如果修订仅涉及文笔润色，不需要更新。
    
    ⚠ 不要将正文保存到 memory/ 目录下，正文由 coordinator 管理。
  context: { revised, review }

session "第{chapter_number}章修订完成。"
  context: { review }
